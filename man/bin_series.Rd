% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bin_series.R
\name{bin_series}
\alias{bin_series}
\title{Bin every series onto a common time grid}
\usage{
bin_series(data, id, time, value, width)
}
\arguments{
\item{data}{A data frame in long form: one row per series and time.}

\item{id, time, value}{Column names in \code{data}, bare or quoted, holding
the series id, the time, and the value. \code{time} and \code{value} must be
numeric.}

\item{width}{A single positive number: the bin width in the units of
\code{time}. Ratings sampled in seconds and binned to 2 Hz use
\code{width = 0.5}.}
}
\value{
A data frame with the \code{id}, \code{time}, and \code{value} columns of
\code{data} (same names), one row per series and non-empty bin, sorted by
series then time.
}
\description{
\code{bin_series()} averages each series within fixed-width time bins, so
series sampled faster than they vary become one value per bin. A time
\code{t} belongs to bin \code{k = floor(t / width)}, which covers
\verb{[k * width, (k + 1) * width)}. The binned row carries the bin midpoint
\code{(k + 0.5) * width} as its time and the arithmetic mean of the series'
non-missing values in the bin as its value. A bin with no non-missing
value for a series yields no row for that series. The bin index is
computed in floating point, so a time that sits on a bin boundary can
fall in the lower bin when \code{t / width} is not exactly an integer (for
example \code{4.3 / 0.1}).
}
\details{
\code{\link[=stat_chromodoris]{stat_chromodoris()}} and \code{\link[=chromodoris]{chromodoris()}} apply the same rule through
their \code{bin} argument.
}
\examples{
d <- data.frame(id = rep(c("a", "b"), each = 4),
                time = rep(c(0, 0.25, 0.5, 0.75), 2),
                value = 1:8)
bin_series(d, id, time, value, width = 0.5)
}
