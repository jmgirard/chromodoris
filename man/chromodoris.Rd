% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chromodoris.R
\name{chromodoris}
\alias{chromodoris}
\title{Draw a Chromodoris plot of many time series}
\usage{
chromodoris(
  data,
  id,
  time,
  value,
  .width = c(0.5, 0.7, 0.9),
  center = c("mean", "median"),
  type = 7,
  bin = NULL
)
}
\arguments{
\item{data}{A data frame in long form: one row per series and time.}

\item{id, time, value}{Column names in \code{data}, bare or quoted, holding
the series id, the time, and the value. \code{time} and \code{value} must be
numeric.}

\item{.width}{Numeric vector of central band widths in (0, 1).
Default \code{c(0.5, 0.7, 0.9)}.}

\item{center}{One of \code{"mean"} (default) or \code{"median"}.}

\item{type}{Quantile algorithm passed to \code{\link[stats:quantile]{stats::quantile()}}. Default 7.}

\item{bin}{\code{NULL} (default) to summarise at each observed \code{x}, or a
single positive number: the bin width in \code{x} units. Each series is
first averaged within bins of that width, exactly as \code{\link[=bin_series]{bin_series()}}
does, and the bands are computed at the bin midpoints.}
}
\value{
A ggplot object with two layers: a ribbon layer built on
\code{\link[=stat_chromodoris]{stat_chromodoris()}} and a line layer for the center.
}
\description{
Summarises many series (one per rater or unit) moment by moment as
nested quantile ribbons around a center line, with a viridis fill
scale, a band legend, and \code{\link[ggplot2:theme_minimal]{ggplot2::theme_minimal()}}. Add further
ggplot2 layers, scales, or themes to the result to customise it.
}
\examples{
set.seed(1)
d <- expand.grid(id = 1:20, time = 1:40)
d$value <- sin(d$time / 6) + rnorm(nrow(d), sd = 0.5)
chromodoris(d, id, time, value)
chromodoris(d, "id", "time", "value", .width = c(0.5, 0.95),
            center = "median")
}
